---
  - name: run roles
    hosts: "{{ DO_ON }}"
    become: yes

    roles:
      - pve-no-subscription-repository
      - dist-upgrade
      - install-packages
      - install-lm-sensors
      - install-docker
      - install-python-pip #ansible